version: "2"

services: 
    hexoNginx:
        restart: always
        image: hexo-nginx
        user: root
        ports: 
            - 80:80
        volumes: 
            - /root/nginx/nginx.conf:/etc/nginx/nginx.conf
            - /root/nginx/conf.d:/etc/nginx/conf.d